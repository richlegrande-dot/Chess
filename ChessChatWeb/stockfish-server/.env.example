# Stockfish Server - Environment Variables

# Port to run server on (default: 3001)
PORT=3001

# API key for authentication (REQUIRED - change this in production!)
# Generate a strong random key: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
STOCKFISH_API_KEY=development-key-change-in-production

# Node environment
NODE_ENV=development
